
<!--
puts 'hello'
str = "<p>hello how <a>Click me</a></p>"
a= /<a[^>]*>([^<]+)<\/a>/.match str
puts a
str = str.gsub(/<a[^>]*>([^<]+)<\/a>/, "")
puts str

#anchor start /<a [^>]*>/
  #anchor end /<\/a>/
  #img src /<img[^>]+src="([^">]+)"/
  #further getting image src /(http|https)([^"]+)/
-->
<div class="container-fluid">
  <div class="jumbotron"><h1 class="text-center">All the latest news</h1></div>
  <% @feeds.each do |feed|%>

  <div class="row">
    <div class="col-md-6"><h2><%= feed.title%></h2></div>
  </div>
      <div class="row">
      <% feed.entries.each do |news|%>

        <div class="col-md-4">
          <div class="thumbnail">
            <img src="<%=news.image%>" alt="...">
            <div class="caption">
              <h3><%= link_to news.title, news.url %></h3>
              <div><%= news.summary.to_s%> </div>
              <p style="margin-top: 10px;margin-bottom: 10px;"><a  href="#" class="btn btn-success pull-right" role="button">Share</a></p>
            </div>
          </div>
        </div>

      <%end%>
      </div>
  <%end%>
</div>